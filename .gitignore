# Dependencies
node_modules/

# Build output
dist/
.output/
.wrangler/
.tanstack/

# Database — real data lives only on the machine running the server,
# never in the repo. The schema itself is version-controlled (src/db).
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Environment variables
.env
.env.local
.env.*.local

# Editor/OS
.DS_Store
*.log
npm-debug.log*

# TypeScript build info
*.tsbuildinfo
